<%- include("partials/header") %>
<div class="flex justify-between items-center mb-6">
  <h1>Hello</h1>
  <button
    class="bg-emerald-500 px-4 py-2 rounded-md text-white hover:bg-emerald-600"
  >
    <a href="/compose">COMPOSE</a>
  </button>
</div>

<%= startingContent %> <% if (posts.length > 0) { %>

<p>Recent Posts</p>
<% } %> <% posts.forEach(post => { %>
 <h2><%= post.title %></h2>
 <% if (post.body.length > 100) { %>
                <p><%= post.body.substring(0, 300) + "..." %></p>
              
                    <a href="/posts/<%= post.title %>">Read More</a>
                    <!-- <button class="like-button bg-emerald-500 px-3 py-1 rounded-md text-white hover:bg-emerald-600" data-post-id="<%= post.id %>" type="button">Like: <span class="like-count"><%= post.likes %></span></button> -->
                </div>
            <% } else { %>
                <p><%= post.body %></p>
                <!-- <div class="flex justify-left gap-4 items-center mt-4">
                    <button class="like-button bg-emerald-500 px-3 py-1 rounded-md text-white hover:bg-emerald-600" data-post-id="<%= post.id %>" type="button">Like: <span class="like-count"><%= post.likes %></span></button>
                </div> -->
            <% } %>

<% }); %>

<script src="/like.js"></script>
<%- include("partials/footer") %>
